-- 撤销「在聚合分组目录里手工声明模型」这条路径（迁移 248 引入）。
--
-- 背景：手工行只绑定到"平台"，同平台的所有账号都会成为候选，无法表达"这个图片模型
-- 由哪个账号提供"。正确做法是把图片模型写在账号自己身上（model_mapping，见账号管理的
-- 「添加图片账号」），聚合目录通过账号映射自动发现，天然按账号绑定。
--
-- 迁移 248 已经在部分环境执行过（migrations 表有记录），这里按前向迁移删列，
-- 不做文件回滚。
ALTER TABLE agent_group_models
    DROP COLUMN IF EXISTS manual;
