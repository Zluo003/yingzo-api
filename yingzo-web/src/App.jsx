import { useEffect, useState } from 'react'
const API = import.meta.env.VITE_API_BASE || '/api/v1'
const nav = [['home','首页'],['keys','API Key 管理'],['usage','使用记录'],['profile','个人资料'],['recharge','充值']]
async function request(path, options={}){const token=localStorage.getItem('auth_token');const res=await fetch(`${API}${path}`,{credentials:'include',headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) ,...(options.headers||{})},...options});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.message||data.error||'请求失败');return data}
function Logo(){return <img className="logo" src="/assets/yingzo-logo.png" alt="Yingzo"/>}
function Shell({page,setPage,children,user}){return <div className="app-shell"><aside><Logo/><div className="eyebrow">YINGZO / CREATIVE AGENT</div><nav>{nav.map(([id,label])=><button key={id} className={page===id?'active':''} onClick={()=>setPage(id)}>{label}</button>)}</nav><div className="aside-note">让想法自然生长<br/><small>Stories · Images · Video</small></div><button className="user-chip" onClick={()=>setPage('profile')}><span>{(user?.name||'Y').slice(0,1)}</span>{user?.name||'创作者'}</button></aside><main>{children}</main></div>}
function Home({setPage}){return <div className="home"><header><Logo/><div className="top-links"><button onClick={()=>setPage('login')}>登录</button><button className="dark" onClick={()=>setPage('register')}>开始创作 →</button></div></header><section className="hero"><div className="hero-copy"><p className="kicker">YINGZO / CREATIVE AGENT</p><h1>让想法<br/><em>自然生长。</em></h1><p className="lead">从一句话开始，创作故事、图片、视频与完整的内容作品。</p><div className="hero-actions"><button className="dark" onClick={()=>setPage('register')}>开始创作 →</button><button className="quiet" onClick={()=>setPage('login')}>登录工作区</button></div></div><div className="hero-art"><img src="/assets/hero-still-life.png" alt="Yingzo creative mood"/></div></section><section className="principles">{[['01','对话式创作','从灵感开始，不被固定模板限制'],['02','多模态表达','文字、图片、视频与电商内容一体完成'],['03','持续一致','Canon、素材版本和人工审核守住创作方向'],['04','本地优先','项目与资产留在你的电脑里']].map(x=><article key={x[0]}><span>{x[0]}</span><h3>{x[1]}</h3><p>{x[2]}</p></article>)}</section></div>}
function Auth({mode,setPage,setUser}){const login=mode==='login';const [form,setForm]=useState({name:'',email:'',password:''});const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');try{const data=await request(login?'/auth/login':'/auth/register',{method:'POST',body:JSON.stringify(login?{email:form.email,password:form.password}:{name:form.name,email:form.email,password:form.password})});if(data.access_token)localStorage.setItem('auth_token',data.access_token);if(data.refresh_token)localStorage.setItem('refresh_token',data.refresh_token);if(data.expires_in)localStorage.setItem('token_expires_at',String(Date.now()+data.expires_in*1000));localStorage.setItem('auth_user',JSON.stringify(data.user||data));setUser(data.user||data);if((data.user||data).role==='admin'){window.location.href=import.meta.env.VITE_ADMIN_URL||'http://127.0.0.1:5174/admin/index.html'}else setPage('keys')}catch(err){setError(err.message)}finally{setBusy(false)}}return <div className="auth-page"><Logo/><div className="auth-card"><p className="kicker">YINGZO / CREATIVE AGENT</p><h1>{login?'欢迎回来。':'开始你的创作。'}</h1><p className="muted">{login?'进入你的本地创作工作区。':'让一个想法，长成完整作品。'}</p><form onSubmit={submit}>{!login&&<label>姓名<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label>}<label>邮箱<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>密码<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label>{error&&<div className="error">{error}</div>}<button className="dark full" disabled={busy}>{busy?'处理中…':login?'登录':'创建账户'} →</button></form><button className="text-link" onClick={()=>setPage(login?'register':'login')}>{login?'还没有账户？创建账户':'已有账户？返回登录'}</button></div></div>}
function Page({title,desc,children}){return <><div className="page-head"><div><p className="kicker">YINGZO / WORKSPACE</p><h1>{title}</h1><p className="muted">{desc}</p></div></div>{children}</>}
function Keys(){const [keys,setKeys]=useState([]);const [loading,setLoading]=useState(true);const [creating,setCreating]=useState(false);useEffect(()=>{request('/keys?page=1&page_size=10').then(d=>setKeys(d.items||d.data||[])).catch(()=>{}).finally(()=>setLoading(false))},[]);async function createKey(){const name=window.prompt('请输入 Key 名称','Yingzo Agent');if(!name)return;setCreating(true);try{const k=await request('/keys',{method:'POST',body:JSON.stringify({name})});setKeys([k,...keys])}finally{setCreating(false)}}return <Page title="API Key 管理" desc="为 Yingzo Agent 创作工作区管理访问凭证。"><div className="toolbar"><button className="dark" onClick={createKey} disabled={creating}>{creating?'创建中…':'创建 Key +'}</button></div><div className="panel"><div className="table-head"><span>名称</span><span>Key</span><span>创建时间</span><span>状态</span></div>{loading?<div className="empty">正在加载…</div>:keys.length?keys.map(k=><div className="table-row" key={k.id}><span>{k.name||'Yingzo Agent'}</span><span>yk_••••••••{String(k.id).slice(-4)}</span><span>{k.created_at||'—'}</span><span className="status">● Active</span></div>):<div className="empty"><strong>还没有 API Key</strong><p>创建一个 Key，让 Yingzo Agent 连接你的创作工作区。</p><button className="dark" onClick={createKey}>创建第一个 Key</button></div>}</div></Page>}
function Usage(){const [stats,setStats]=useState(null);const [rows,setRows]=useState([]);useEffect(()=>{Promise.all([request('/usage/dashboard/stats'),request('/usage?page=1&page_size=20')]).then(([a,b])=>{setStats(a);setRows(b.items||b.data||[])}).catch(()=>{})},[]);return <Page title="使用记录" desc="查看创作请求、媒体生成与余额消耗。"><div className="stats"><div><small>本月请求</small><strong>{stats?.total_requests??'—'}</strong></div><div><small>生成 Tokens</small><strong>{stats?.total_tokens??'—'}</strong></div><div><small>本月消耗</small><strong>{stats?.total_cost!=null?`$${Number(stats.total_cost).toFixed(2)}`:'—'}</strong></div></div><div className="panel chart"><div className="panel-title">创作活动趋势 <span>最近 30 天⌄</span></div><div className="bars">{Array.from({length:28},(_,i)=><i key={i} style={{height:`${18+(i*17)%92}px`}}/>)}</div></div><div className="panel"><div className="table-head"><span>时间</span><span>类型</span><span>模型</span><span>消耗</span></div>{rows.length?rows.map((r,i)=><div className="table-row" key={r.id||i}><span>{r.created_at||'—'}</span><span>{r.request_type||'请求'}</span><span>{r.model||'—'}</span><span>{r.cost!=null?`$${r.cost}`:'—'}</span></div>):<div className="empty">暂无使用记录</div>}</div></Page>}
function Profile(){const [profile,setProfile]=useState({username:'',email:''});const [saved,setSaved]=useState(false);useEffect(()=>{request('/user/profile').then(d=>setProfile(d.data||d)).catch(()=>{})},[]);async function save(){await request('/user',{method:'PUT',body:JSON.stringify({username:profile.username})});setSaved(true)}return <Page title="个人资料" desc="管理你的身份、偏好与安全设置。"><div className="form-grid"><div className="panel form-card"><h2>基本信息</h2><label>姓名<input value={profile.username||''} onChange={e=>setProfile({...profile,username:e.target.value})} placeholder="你的名字"/></label><label>邮箱<input value={profile.email||''} readOnly placeholder="you@example.com"/></label><button className="dark" onClick={save}>{saved?'已保存':'保存更改'} →</button></div><div className="panel form-card"><h2>密码</h2><label>当前密码<input type="password" placeholder="••••••••"/></label><label>新密码<input type="password" placeholder="输入新密码"/></label><button className="outline">更新密码</button></div></div></Page>}
function Recharge(){const [info,setInfo]=useState(null);const [amount,setAmount]=useState(50);const [orders,setOrders]=useState([]);useEffect(()=>{Promise.all([request('/payment/checkout-info'),request('/payment/orders/my?page=1&page_size=10')]).then(([a,b])=>{setInfo(a.data||a);setOrders(b.items||b.data||[])}).catch(()=>{})},[]);async function pay(){const result=await request('/payment/orders',{method:'POST',body:JSON.stringify({amount,method:info?.methods?.[0]?.type||'stripe'})});if(result.pay_url) window.location.href=result.pay_url}return <Page title="充值" desc="为你的创作余额充值，继续把想法变成作品。"><div className="recharge-grid"><div className="panel form-card"><small>当前余额</small><div className="balance">${info?.balance??'0.00'}</div><h2>选择金额</h2><div className="amounts">{[10,50,100,200].map(x=><button className={amount===x?'selected':''} key={x} onClick={()=>setAmount(x)}>${x}</button>)}</div><label>自定义金额<input type="number" value={amount} onChange={e=>setAmount(Number(e.target.value))} placeholder="输入金额"/></label><button className="dark full" onClick={pay}>继续支付 →</button></div><div className="panel"><h2>充值记录</h2>{orders.length?orders.map(o=><div className="table-row" key={o.id}><span>{o.created_at}</span><span>充值</span><span>${o.amount}</span><span>{o.status}</span></div>):<div className="empty">还没有充值记录</div>}</div></div></Page>}
export function App(){const [page,setPage]=useState('home');const [user,setUser]=useState(()=>{try{return JSON.parse(localStorage.getItem('auth_user')||'null')}catch{return null}});if(page==='login'||page==='register')return <Auth mode={page} setPage={setPage} setUser={setUser}/>;return page==='home'?<Home setPage={setPage}/>:<Shell page={page} setPage={setPage} user={user}>{page==='keys'?<Keys/>:page==='usage'?<Usage/>:page==='profile'?<Profile/>:<Recharge/>}</Shell>}
